
// import { Bot } from "./bot";
// import { ServiceDefinition } from "./infrastructure/mesh";

// import tomcat from ".";

// tomcat.getHostBuilder("test").addMeshService(cfg => cfg.userServiceConstructor(new ServiceDefinition('strategy', { interval: "1m" }), {}, (def) => { return new Bot(def) }))
// tomcat.getBotBuilder('test').addBot(b => {
//     (b);


// })
